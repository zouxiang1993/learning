index	0	1	2	3	4	5       表示一个节点的编号

 base	一个偏移量，即这个节点的所有子节点的基础偏移量

check 			表示该节点的父节点的base值。


base[s] : 子节点的基础偏移量
ch : 字符偏移量

子节点的基础偏移量 + 字符偏移量 = 子节点的下标
即 base[s] + ch = t

在子节点处，
check[t] = base[s] (也可能是-s,负号表示这是一个叶子节点)


base[0] = 1;

在每个词的最后添加一个code=0表示的词尾节点，这样就使得不会有一个单词是另一个单词的前缀这样的情况发生。